DB_HOST=localhost
DB_PORT=5432
DB_NAME=my-db
DB_USERNAME=admin  # Keep this instead of DB_USER for consistency
DB_PASSWORD=admin  # Consider a stronger password
DB_URL=postgres://${DB_USERNAME}:${DB_PASSWORD}@${DB_HOST}:${DB_PORT}/${DB_NAME}

# Authentication
JWT_SECRET_KEY=Change_ME!!!  # Replace with a strong secret in production
BCRYPT_SALT=10
JWT_EXPIRATION=2d

# Server
PORT=3000
SERVICE_NAME=Meal Subscription Service

# Logging & Monitoring
LOG_LEVEL=info
LOG_REQUEST=false
PINO_PRETTY=false

# Tracing
JAEGER_AGENT_HOST=jaeger
JAEGER_AGENT_PORT=16686
OTEL_COLLECTOR_HOST=localhost
OTEL_COLLECTOR_PORT_GRPC=4317
OTEL_COLLECTOR_PORT_HTTP=4318
OTEL_EXPORTER_OTLP_ENDPOINT=http://${OTEL_COLLECTOR_HOST}:${OTEL_COLLECTOR_PORT_GRPC}